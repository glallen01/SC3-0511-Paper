\documentclass[
	titlepage,
	12pt,
	letterpaper,
	headings=small,
	%twoside,
	oneside,
	%parskip=half-,
	parindent,
	%liststotoc,
	%draft,
	pdftex]{scrartcl}

\usepackage[utf8x]{inputenc}	% for windows quotes without having to sed them out
% or
% cat test.txt| sed -e's/-/--/g' -e's/“/\`\`/g' -e``s/”/''/g`` -e''s/…/\\\ldots /g`` -e''s/$/\n/`` -e''s/’/'/g`` | fmt --width=80
%
\usepackage{microtype}
\usepackage[margin=1in,letterpaper]{geometry}
\usepackage{setspace}		% turns off in floats and tables
%\doublespacing

% For Times (Nimbus Roman) Font:
\usepackage[T1]{fontenc}
%\usepackage{mathptmx}
\usepackage{tgtermes}

%\usepackage{pdftex}
%\usepackage[pdftex]{graphicx}
\usepackage{graphicx}

\usepackage{jurabib}
\usepackage[pdftex]{hyperref}
\usepackage{wrapfig}

\usepackage{makeidx}
\makeindex

\usepackage{lipsum}		% delete this later

\usepackage[small,compact]{titlesec}
\usepackage{color}

%\usepackage{booktabs}	% Remove one of these and convert the others
\usepackage{ctable}	%

%----- local definitions ----
\newcommand{\red}[1]{\textcolor{red}{#1}}

\setlength{\headheight}{1.1\baselineskip}
\usepackage{scrpage2} % vs. fancyhdr for komascript

\newenvironment{blkquote}[1][1]
  { \begin{quote}\begin{spacing}{ #1}\small }
  { \end{spacing}\end{quote} \vspace{-24pt} }

\deffootnote{1.5em}{1em}{%
  \makebox[1.5em][l]{\thefootnotemark}}


%%% headings
% \usepackage{fancyhdr}
% \pagestyle{fancy}
% \lhead{SCCC 05-11A}
% \chead{}
% \rhead{\thepage}
% \lfoot{}
% \cfoot{}
% \rfoot{}
% \renewcommand{\headrulewidth}{0.4pt}
%%%


%\jurabibsetup{
%	citefull=first,
%	ibidem=strict,
%	commabeforerest,
%}
\jurabibsetup{
	% bibformat=compress,%
	% bibformat=nohang,
	%authorformat=year,
	%authorformat={year,and},	% for \cite[p. 1]{foo} -> (Foo 2001, p. 1)
	%super,			% makes all \cite into \footcite
        %authorformat=smallcaps,
        %pages=format,%
%        titleformat=all,%
%        titleformat=italic,%
%        ibidem=strict,%
	annote,
	authorformat=and,		% for \cite[p. 1]{foo} -> (Foo, p. 1)
	bibformat=ibidem,
	bibformat=raggedright,
	dotafter=bibentry,
	hypercite=false,	% diables turning all \cite into reference with hyperref enabled
        commabeforerest,%
        round,
        titleformat=commasep,%
}

\renewcommand{\bibansep}{, }                    % Want a comma instead of a colon after the Author:
\renewcommand{\bibauthormultiple}{------~}
\renewcommand{\bibtfont}{\textit}
\renewcommand{\bibbtfont}{\textit}
\bibAnnotePath{{annotations/}}
\renewcommand{\refname}{Annotated Bibliography}
\renewcommand*{\jbannoteformat}[1]{{\fontsize{12pt}{1.2 \times 12pt}\begin{quote}#1\end{quote}}}

%\jbyearaftertitle
%\renewcommand{\jbcitationyearformat}[1]{#1}
%\renewcommand{\bibansep}{, }
%\renewcommand{\bibatsep}{.}
%\renewcommand{\bibapifont}{\textit}
%\renewcommand{\biblnfont}{}
%\renewcommand{\bibfnfont}{}
%\renewcommand{\bibelnfont}{}
%\renewcommand{\bibefnfont}{}
%\renewcommand{\bibtfont}{\textit}
%\renewcommand{\bibbtfont}{\textit}
%\renewcommand{\bibjtfont}{}

