\documentclass[
	titlepage,
	12pt,
	letterpaper,
	headings=small,
	twoside,
	oneside,
	%parskip=half-,
	parindent,
	%liststotoc,
	%draft,
	pdftex]{scrartcl}

\usepackage[utf8x]{inputenc}	% for windows quotes without having to sed them out
% or
% cat test.txt| sed -e's/-/--/g' -e's/“/\`\`/g' -e``s/”/''/g`` -e''s/…/\\\ldots /g`` -e''s/$/\n/`` -e''s/’/'/g`` | fmt --width=80
%


\usepackage{microtype}
\usepackage[margin=1in,letterpaper]{geometry}
\usepackage{setspace}		% turns off in floats and tables
\doublespacing

% For Times (Nimbus Roman) Font:
\usepackage[T1]{fontenc}
%\usepackage{mathptmx}
\usepackage{tgtermes}

%\usepackage{pdftex}
%\usepackage[pdftex]{graphicx}
\usepackage{graphicx}
\usepackage{float}
\usepackage{wrapfig}

\usepackage{ragged2e}		% recommended by jurabib for better ragged right
\usepackage{wrapfig}

\usepackage{makeidx}
\makeindex

\usepackage{lipsum}		% delete this later

\usepackage[small,compact]{titlesec}
\usepackage{color}

%\usepackage{booktabs}	% Remove one of these and convert the others
\usepackage{ctable}	%
\usepackage{multirow}

%----- local definitions ----
\newcommand{\red}[1]{\textcolor{red}{#1}}

\setlength{\headheight}{1.1\baselineskip}
\usepackage{scrpage2} % vs. fancyhdr for komascript

\newenvironment{blkquote}[1][1]
  { \begin{quote}\begin{spacing}{ #1}\small }
  { \end{spacing}\end{quote} \vspace{-24pt} }

%\renewenvironment{quote}{%
%  \list{}{%
%    \leftmargin0.5cm   % this is the adjusting screw
%    \rightmargin\leftmargin\begin{spacing}{1.3}
%  }
%  \item\relax
%}
%{\end{spacing}\endlist}

\let\oldquote=\quote
\let\oldendquote=\endquote
\renewenvironment{quote}
  {\vspace{-1em}\begin{singlespace}\oldquote}
  {\oldendquote\end{singlespace}\vspace{3pt}}

\deffootnote{1.5em}{1em}{%
  \makebox[1.5em][l]{\thefootnotemark}}


%%% headings
% \usepackage{fancyhdr}
% \pagestyle{fancy}
% \lhead{SCCC 05-11A}
% \chead{}
% \rhead{\thepage}
% \lfoot{}
% \cfoot{}
% \rfoot{}
% \renewcommand{\headrulewidth}{0.4pt}
%%%
